# Argus Recovery Requirement Matrix

This matrix is the recovery gate for the server-mode implementation. It is sourced from the shared Argus product docs under `/Users/mike/shared-workspace/subspace/` and from no host-topology documents.

| Source doc | Requirement | Implementation evidence |
| --- | --- | --- |
| `/Users/mike/shared-workspace/subspace/specs/argus-server-requirements.md` section 1 | Argus publishes well-formed, source-grounded, embedded news reports into Subspace; subscribers perform interpretation and filtering. | Package payloads keep source/provenance fields and canonical `supplied_embeddings`; docs describe downstream receivers as separate from Argus publishing. |
| `/Users/mike/shared-workspace/subspace/specs/argus-server-requirements.md` section 3.1 | Argus publishes to the approved Subspace target using its configured publisher identity after live publishing approval. | `PublishConfig` uses `subspace_endpoint`, `subspace_websocket_path`, `subspace_agent_id`, and env-only `ARGUS_SUBSPACE_SESSION_TOKEN`; active mode remains gated by `publish.state` and `live_approval`. |
| `/Users/mike/shared-workspace/subspace/specs/argus-server-requirements.md` section 8.1 | Argus is a Subspace publisher; its responsibility ends at submitting a valid message and recording the resulting Subspace message id. | `post_message_to_subspace` joins the Subspace firehose and posts `post_message`; publish attempts record `subspace_message_id` on success. |
| `/Users/mike/shared-workspace/subspace/specs/argus-server-requirements.md` section 8.1 | Argus must not require receiver-side components, local subscribers, or desktop runtime to publish. | The runtime publish path no longer calls receiver-side socket/API helpers; legacy publish topology fields are rejected by config loading. |
| `/Users/mike/shared-workspace/subspace/specs/argus-server-requirements.md` section 8.2 | Live payloads should preserve canonical `supplied_embeddings` and avoid camelCase fallback fields. | Tests assert package `supplied_embeddings`, forwarded embedding `space_id`, and no `suppliedEmbeddings`. |
| `/Users/mike/shared-workspace/subspace/specs/argus-server-requirements.md` sections 6 and 9 plus T261 product correction | Publish path must suppress exact duplicate reports without overfitting brittle dedupe or routing interpretation; prefer false negatives over false positives; same normalized canonical URL across feeds is a conservative exact duplicate, while feed GUIDs are not semantic story identity. | URL-first cross-source dedupe collapses same canonical URLs to one package, records all carrying sources in package provenance, keeps GUID conflicts when URLs/titles differ, and records ambiguity instead of suppressing. |
| `/Users/mike/shared-workspace/subspace/specs/argus-ingestion-implementation-spec.md` product invariants | Argus readiness must use ingestion, normalization, dedupe, source health, and publish-candidate evidence; digest output is not an acceptance artifact. | Deploy readiness now names product-valid artifacts only and explicitly excludes digest files as readiness proof. |
| `/Users/mike/shared-workspace/subspace/specs/argus-ingestion-implementation-spec.md` source and parser requirements | V0 uses the verified 13-feed source set with RSS, Atom, and arXiv Atom parsing. | Checked-in config still loads 13 enabled sources; tests cover RSS `GMT` dates and Atom snippets for The Verge/Reddit style feeds. |
| `/Users/mike/shared-workspace/subspace/specs/argus-ingestion-implementation-spec.md` dedupe requirements plus T261 product correction | Feed GUIDs may be used only as source-local replay bookkeeping when stronger URL evidence is unavailable; cross-source suppression is limited to exact normalized canonical URL matches unless a future explicit safe publisher/domain/group rule exists. | Tests cover source-local URL dedupe, same-URL cross-source collapse with carrier provenance, same-GUID/different-URL preservation, same-source GUID ambiguity recording, and baseline storage for newly added sources. |
| `/Users/mike/shared-workspace/subspace/notes/argus-implementation-dispatch-brief.md` acceptance checks | Publish candidates must expose source, source class, canonical URL, timestamps, stable ids, dedupe identity, and provenance metadata. | Existing server/pipeline tests cover package shape, provenance, canonical URL, timestamps, source class, and dedupe metadata. |
| `/Users/mike/shared-workspace/subspace/notes/argus-implementation-verification-plan.md` hard stops | Do not call the slice ready if Argus requires live publishing, cron, deployment, subscriber views, digest output, or modifies live services/config. | No live Subspace send, deployment, service start, cron, production config activation, or digest-readiness claim was performed by this work. |
| `/Users/mike/shared-workspace/subspace/notes/argus-implementation-verification-plan.md` test checklist | Tests must cover config validation, RSS/Atom/arXiv parsing, source-local dedupe, failure handling, provenance/community labeling, and artifact validity. | Full unittest discovery covers the existing pipeline/server suites plus direct Subspace publisher tests and legacy topology rejection. |
